# 빌드 컨텍스트: 프로젝트 루트
CONTEXT=.

# Dockerfile 위치
DOCKERFILE=docker/Dockerfile

# 이미지 이름/태그
IMAGE_NAME=doc-parser-tableformer
IMAGE_TAG=0.0.0

# 푸시할 레지스트리 (없으면 빈값)
REGISTRY=mncregistry:30500

# 빌드 시점에 이미지에 구워둘 TableFormer 모드 (fast | accurate)
MODE=accurate
